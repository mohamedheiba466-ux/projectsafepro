# Required
GEMINI_API_KEY=your_gemini_api_key_here

# Optional (defaults shown)
GEMINI_MODEL=gemini-2.5-pro
ADMIN_PASSWORD=change_me

# Netlify Blobs يعمل تلقائياً على Netlify deployments — لا يحتاج متغيرات.
